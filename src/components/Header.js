export function Header(){
    return(
        <div style={{display:'flex', flexDirection:'row'}}>
            <button>HOME</button>
            <button>FUNCIONÁRIOS</button>
        </div>  
    )
}